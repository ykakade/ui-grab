node_modules
dist

# Per-machine Claude Code settings — .claude/settings.json and
# .claude/commands/ are meant to be committed, this one is not.
.claude/settings.local.json

# Pending picks and the bookkeeping around them. Two files in here are worth
# committing: config.json, which is the team's automation mode, and map.json,
# which is where confirmed picks were found. Everything else is per-machine.
.ui-grab/queue.json
.ui-grab/queue.blocks.json
.ui-grab/sent.json
.ui-grab/batches.json
.ui-grab/shots/

.DS_Store
*.log
